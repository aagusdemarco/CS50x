#include <cs50.h>
#include <stdio.h>

int main(void)
{
    long amex;
    long visa;
    long master;

    do
    {
        long card = get_long("Number: ");
    }
    while(card < 0);

    for (int i = 0; i >)
}
